{#
/**
 * @file locator_entity.html.twig
 * Default theme implementation to present Locator Entity data.
 *
 * This template is used when viewing Locator Entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_locator_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('locator_entity') }}>
    {% if content %}
        {{- content -}}
    {% endif %}
</div>
